<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/searchView"/> <!-- Fondo blanco, cámbialo si es necesario -->
    <stroke
        android:width="1dp"
        android:color="@color/search_border"/> <!-- Borde de 2dp de ancho -->
    <corners android:radius="0dp"/> <!-- Esquinas redondeadas de 8dp (opcional) -->
</shape>